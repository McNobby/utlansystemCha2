import React from 'react'

const EditUser = (props) => {

    const {name, mail, shown, setShown} = props

    return (
        <div className={shown}>
            
        </div>
    )
}

export default EditUser
